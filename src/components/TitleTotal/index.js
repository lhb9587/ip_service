import TitleTotalConponent from '@/components/TitleTotal/index.vue';

const TitleTotal = {
  install: function(Vue) {
    Vue.component('TitleTotal', TitleTotalConponent);
  }
}

export default TitleTotal
